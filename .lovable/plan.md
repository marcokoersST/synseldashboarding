

# Make TV Mode Period Badge Larger

### Change in `src/pages/TVRanglijsten.tsx` (line 194)

Increase the Badge size from `text-sm` to `text-lg` with more padding:

```tsx
<Badge variant="default" className="text-lg font-semibold px-4 py-1">
```

