Wrap the calls-row `ExternalLink` icon (line 216 in `src/components/manager/lcb/CandidateDetailPane.tsx`) in an `<a href="https://ai.synsel.nl/recordings" target="_blank" rel="noopener noreferrer">` so each call's Link cell becomes a clickable placeholder hyperlink. Add hover styling (text-foreground/primary) and `stopPropagation` on click. No other behavior or layout changes.